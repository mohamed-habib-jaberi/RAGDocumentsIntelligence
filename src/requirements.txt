# Framework HTTP : définit les routes et les contrats de l'API.
fastapi==0.110.2
# Serveur ASGI : exécute l'application FastAPI en développement et en production.
uvicorn[standard]==0.29.0
# Prise en charge des formulaires multipart, nécessaire aux futurs uploads de fichiers.
python-multipart==0.0.9
# Charge les variables locales de .env au démarrage, sans exposer les secrets dans Git.
python-dotenv==1.0.1
# Settings model that validates values loaded from .env.
pydantic-settings==2.2.1
# Non-blocking file writes used by the upload endpoint.
aiofiles==23.2.1
# Provides document loaders and the recursive text splitter for RAG preparation.
langchain==0.1.20
# PDF parser used by the PyMuPDF document loader.
PyMuPDF==1.24.3
# Asynchronous MongoDB driver used by FastAPI route handlers.
motor==3.4.0
# Pydantic helpers and BSON ObjectId support for MongoDB documents.
pydantic-mongo==2.3.0
