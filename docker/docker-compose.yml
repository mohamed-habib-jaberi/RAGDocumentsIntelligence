services:
  mongodb:
    image: mongo:7-jammy
    container_name: rag-document-intelligence-mongodb
    ports:
      - "27007:27017"
    volumes:
      - ./mongodb:/data/db
    restart: unless-stopped
